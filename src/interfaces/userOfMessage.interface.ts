/* eslint-disable prettier/prettier */
/**
 * Interface for UserOfMessage
 * no password, no email, no codePassword
 */
export interface UserOfMessage {
  _id: string;
  firstName: string;
  lastName: string;
  photo: string;
}
